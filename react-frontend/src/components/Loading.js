import React from 'react';

const Loading = () => (
  <div className="modern-loader-ret">
    <div className="modern-loader">
      <span>Loading...</span>
    </div>
  </div>
);

export default Loading;
